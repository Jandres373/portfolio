---
import { Icon } from 'astro-icon/components';
import { SITE } from '../../utils/config';
import { getHomePermalink } from '../../utils/permalinks';

interface Link {
  text?: string;
  href?: string;
  ariaLabel?: string;
  icon?: string;
}

interface Links {
  title?: string;
  links: Array<Link>;
}

export interface Props {
  links: Array<Links>;
  secondaryLinks: Array<Link>;
  socialLinks: Array<Link>;
  footNote?: string;
  theme?: string;
}

const { socialLinks = [], secondaryLinks = [], links = [], footNote = '', theme = 'light' } = Astro.props;
---

<footer class:list={[{ dark: theme === 'dark' }, 'relative border-t border-gray-200 dark:border-slate-800 not-prose']}>
  <div class="dark:bg-dark absolute inset-0 pointer-events-none" aria-hidden="true"></div>
  <div class="relative max-w-full mx-auto px-4 sm:px-6 dark:text-slate-300">
    <div class="flex flex-col">
      <div class="container mx-auto px-4 md:px-6 lg:px-8">
        <div class="grid mt-5 grid-cols-1 gap-8 md:grid-cols-2 md:gap-12 lg:grid-cols-4 lg:gap-16">
          <div class="space-y-4">
            <h4 class="text-lg font-semibold">About me</h4>
            <p class="text-sm">
              JAM WEB DEV develops initiatives using AI and full-stack development to contribute to solving general
              community issues.
            </p>
            <div class="flex space-x-4">
              <a
                class="text-gray-400 transition-colors hover:text-gray-50"
                href={'https://www.linkedin.com/in/julian-mosquera-09762029a/'}
              >
              </a>
            </div>
          </div>
          <div class="space-y-4">
            <h4 class="text-lg font-semibold">Check my projects</h4>
            <ul class="space-y-2">
              <li>
                <a class="text-gray-400 transition-colors hover:text-gray-50" href="#"> CoCode</a>
              </li>
              <li>
                <a class="text-gray-400 transition-colors hover:text-gray-50" href="#"> PDF Reader</a>
              </li>
              <li>
                <a class="text-gray-400 transition-colors hover:text-gray-50" href="#"> AI Learning assistant</a>
              </li>
              <li>
                <a class="text-gray-400 transition-colors hover:text-gray-50" href="#"> Gestures recognition</a>
              </li>
            </ul>
          </div>
          <div class="space-y-4">
            <h4 class="text-lg font-semibold">Contact me</h4>
            <ul class="space-y-2">
              <li>
                <div class="flex items-center space-x-2">
                  <span>Not available</span>
                </div>
              </li>
              <li>
                <div class="flex items-center space-x-2">
                  <span>jam.webdesign.01@gmail.com</span>
                </div>
              </li>
              <li>
                <div class="flex items-center space-x-2">
                  <span>Medellín, CO.</span>
                </div>
              </li>
            </ul>
          </div>
          <div class="space-y-4">
            <h4 class="text-lg font-semibold">Subscribe</h4>
            <p>Subscribe now to stay updated with the latest news and updates about my projects!</p>
            <form class="flex space-x-2"></form>
          </div>
        </div>
        <div class="mt-8 text-center text-sm md:mt-12">© 2024 JAM WEB DEV.</div>
      </div>
    </div>
  </div>
</footer>
